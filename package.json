{
  "name": "bastiongate-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test web/*.test.js",
    "test:e2e": "playwright test"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0"
  }
}
